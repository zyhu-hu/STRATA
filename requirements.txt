# Plain-pip dependencies. Three packages need flags a requirements file
# cannot express and are installed as separate `make install` steps (see the
# Makefile and docker/Dockerfile):
#   - natten     (wheel must match the installed torch: -f https://whl.natten.org,
#                 or the source build in docker/build_natten.sh on NGC images)
#   - earth2grid (not on PyPI; pinned GitHub archive, --no-deps)
#   - nvidia-physicsnemo (pinned GitHub archive with the Strata models;
#                 --no-deps so its torch>=2.10.0 floor cannot replace a
#                 container's pre-release torch build)
# lightning >=2.6.6 everywhere: earlier releases (through 2.6.5) import and
# execute arbitrary module paths from a checkpoint's _instantiator
# hyperparameter in LightningModule.load_from_checkpoint (fixed upstream by
# an instantiator allowlist). pytorch-lightning is the same code under its
# second distribution name and must be pinned explicitly — upgrading
# `lightning` alone leaves a vulnerable pytorch_lightning importable.
lightning==2.6.6
pytorch-lightning==2.6.6
multi-storage-client[boto3]>=0.14.1
torchmetrics>=1.9.0
# to avoid errors
boto3==1.35.23
python-dotenv>=1.2.2
s3fs>=2025.10.0
xarray>=2026.7.0
cartopy>=0.25.0
lmdb>=2.3.0
pytorch-optimizer>=3.10.1
netCDF4>=1.7.4
typer>=0.26.8
wandb>=0.28.0
zarr>=3
